  r2dbc:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

# Otras configuraciones como el puerto, etc.
server:
  port: 8080

springdoc:
  api-docs:
    path: /swagger-ui

